export {Statuses, urls} from './constants'

const Statuses = {
    INITIAL: "initial",
    FAILED: "failed",
    SUCCESSFUL: "successful",
  };

const urls = {
    punk:"https://api.punkapi.com/v2/beers"
}  